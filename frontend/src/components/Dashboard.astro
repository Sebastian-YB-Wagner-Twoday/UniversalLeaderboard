---
import CreateContestForm from "./CreateContestForm.vue";
import type { Contest } from "../model/contest/Contest.model";
import { get } from "@/lib/api/http";
import LeaderBoardWrapper from "./Leaderboard/Client/LeaderBoardWrapper.astro";

let contests: Contest[];

const pagination = 0;

const data = await get(
  `${import.meta.env.BACKEND_HOST}/user/contests/${pagination}`,
  Astro.cookies.get("session")?.value
);

contests = await data
  .json()
  .catch((e) => console.log("couldn't fetch list: ", e));
---

<div>
  <CreateContestForm client:load />

  <div>
    {
      contests
        ? contests.map((contest) => (
            <div>
              <LeaderBoardWrapper contestId={contest.id} />
            </div>
          ))
        : ""
    }
  </div>
</div>
